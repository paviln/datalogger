<?xml version="1.0" encoding="utf-8" ?>
    
<ContentPage Title="Menu" xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="App.MainPage">

    <StackLayout Padding="8">
        <Grid >
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Clicked="Click" Text="Register Plant" Grid.Row="0" BackgroundColor="DodgerBlue" FontAttributes="Bold"/>
            <Button Command="{Binding SecondPageNavCommand}" Text="Show Data" Grid.Row="1" BackgroundColor="DodgerBlue" FontAttributes="Bold"/>
            <Button Command="{Binding FindPlantPageNavCommand}" Text="Find Plant" Grid.Row="2" BackgroundColor="DodgerBlue" FontAttributes="Bold"/>
            <Button Command="{Binding NotificationPageNavCommand}" Text="Notifications" Grid.Row="3" BackgroundColor="DodgerBlue" FontAttributes="Bold"/>
        </Grid>
        
    </StackLayout>
   
</ContentPage>
